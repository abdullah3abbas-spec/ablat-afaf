# CLAUDE.md — دستور مشروع «منصّة أبلة عفاف»

> هذا الملف يُقرأ تلقائياً في كل جلسة. التزم به حرفياً ولا تخالفه دون إذن صريح.

---

## 0. المبدأ الحاكم — اقرأه قبل كل شيء

اسم المشروع: **منصّة أبلة عفاف**. صاحبته معلّمة علوم في الستين، والمنصّة تُبنى **لراحتها** قبل أي شيء آخر.

> **القاعدة الذهبية: أبلة عفاف لا تُدخل بيانات. هي تفتح، وتختار، وتصوّر.**

ثلاث قواعد مشتقّة منها، لا يجوز مخالفتها:

1. **صفر إدخال يدوي.** كل رقم يدخل النظام يأتي من: **تصوير** ورقة رصد أو كشف أو ورقة اختبار · أو **رفع ملف** PDF/Excel · أو **ضغطة زر واحدة** أثناء الحصة (نجمة، حضور). الكتابة بلوحة المفاتيح استثناء نادر، لا قاعدة.
2. **كل شيء جاهز مسبقاً.** العروض وأوراق العمل والألعاب والتجارب والاختبارات وخطط الدروس تُبنى **قبل** أن تستخدم المنصّة، لا عند الطلب. دورها: تراجع وتطبع.
3. **اختبار الشاشة الواحدة.** أي شاشة تطلب منها كتابة أكثر من سطر واحد = شاشة مصمَّمة خطأ. أعد تصميمها.

**وكن دائماً سابقاً بخطوة:** لا تنتظر أن تطلب. جهّز الاختبار قبل موعده، واطبع لوحة الشرف يوم الخميس وحدك، ونبّهها للطالبة المتعثّرة قبل أن تسأل، واقترح في كل شاشة الخطوة التالية.

---

## 0-ب. تحويل «زكريت» — تعديل معتمد على هذا الدستور (أغسطس ٢٠٢٦)

اعتمد المالك ماسترَ برومبت «مدرسة زكريت الابتدائية للبنات» ونُفّذت مراحله ٠–٥ (انظر `مواصفات/تدقيق-المرحلة-٠.md` و`مواصفات/فحص-المرحلة-٥.md`). ما يلي يعدّل هذا الدستور حيث يتعارضان:

1. **استثناء وحيد من حظر السحابة (§3):** يوجد Worker وسيط واحد على Cloudflare باسم `afaf-ai-gateway` (كوده في `gateway/`) يحمل مفتاحَي Gemini وOpenAI أسراراً على الخادم، ويمرّر **محتوى المنهج فقط**، وفيه حارس ميزانية (50$ إجمالي + 5$ يومي لكل مزوّد) وكاش وحدّ معدّل. **قاعدة «أي شيء فيه اسم بنت لا يخرج من الجهاز» باقية بلا مساس** — يفرضها `aiGuard.ts` وشاشة «ما سيُرسل» وسجل الإرسال. النماذج والأسعار إعدادات في `gateway/wrangler.jsonc` لا كود، و`GET /api/models` يعرض المتاح للمفتاح عند تقادم نموذج.
2. **هيكل التنقّل:** شريط سفلي ثابت بخمسة أقسام (اليوم · المكتبة · الفصول · الأدوات · الإدارة)، والرئيسية «مركز اليوم» بقرارات لا تتجاوز الستة، والمكتبة موحّدة بتبويبات (الدروس · المنهج · ملفاتي · اسألي المنهج).
3. **قدرات جديدة قائمة:** «اسألي المنهج» (إجابة من المصادر فقط مع الاستشهاد) · استوديو العروض البصرية (مسودة ← اعتماد، فاحص جودة يفرض ٦٠٪ بصري و٤٥ كلمة وتفاعلاً كل ٤ شرائح، تصدير PPTX/طباعة) · وضع الفصل للبروجكتور (شرائح + ٥ ألعاب من البنك + اختيار عادل + مؤقّت + فرق) · المختبر التفاعلي (`/lab`).
4. **ألوان الهوية** توكِنات CSS في `:root` بملف `src/index.css` — هوية زكريت الرسمية حين تصل تُطبَّق من هناك وحده.
5. الإجابات وإجابات الأنشطة **لا تظهر للطالبات إلا بضغطة المعلّمة** — قاعدة عامة في كل الشاشات الصفية.

---

## 1. من هي المستخدمة؟

**معلّمة** علوم للمستوى الخامس (المنهج القطري)، عمرها ٦٠ سنة، **ليست مبرمجة ولا تريد أن تصبح مبرمجة**.
تُدرّس **طالبات** (مدرسة بنات).

قواعد التعامل معها — إلزامية:

- خاطبها **بالعربية وبصيغة المؤنث دائماً**: «جاهزة»، «جرّبي»، «أضيفي»، «اختاري». لا تستخدم صيغة المذكّر معها أبداً.
- المصطلحات التقنية اشرحها بكلمة عربية بسيطة بين قوسين.
- **لا تسألها أسئلة تقنية.** لا "أي مكتبة تفضّلين؟" ولا "SQLite أم IndexedDB؟". قرّر أنت بناءً على هذا الملف، ونفّذ، ثم أخبرها بالنتيجة بجملة واحدة.
- اسألها فقط أسئلة **تربوية أو تنظيمية**: كم فصلاً تُدرّسين؟ ما نسبة درجة الواجبات؟ ما شكل الشهادة؟
- عند الانتهاء من أي مهمة، اكتب لها **٣ أسطر بالعربية**: ماذا صار جاهزاً، وكيف تجرّبه، وما الخطوة التالية. لا تعرض عليها كوداً إلا إذا طلبته.
- إذا واجهتك مشكلة تقنية، **حلّها بنفسك** ولا تنقلها إليها. أخبرها فقط إن كان القرار يخصّها.

### ⚠️ الصيغة المؤنثة في كل المخرجات — قاعدة إلزامية

كل نص يظهر للمستخدمة أو يُطبع في مستند يجب أن يكون بصيغة المؤنث، لأن كل المتعلّمات بنات:

| ❌ خطأ | ✅ صواب |
|--------|---------|
| الطالب | **الطالبة** |
| حضر / غاب / تأخّر | **حضرت / غابت / تأخّرت** |
| الطلاب · عدد الطلاب | **الطالبات · عدد الطالبات** |
| ناجح / راسب | **ناجحة / متعثّرة** |
| ولي الأمر | **ولية / ولي الأمر** |
| «تشهد المدرسة بأن الطالب … قد حصل على» | «تشهد المدرسة بأن **الطالبة** … قد **حصلت** على» |
| المتفوقون · الأوائل | **المتفوقات · الأوائل من الطالبات** |
| مستكشف · باحث · عالِم صغير | **مستكشفة · باحثة · عالِمة صغيرة** |
| زميل · مساعدة زميل | **زميلة · مساعدة زميلة** |

ضع كل هذه النصوص في **ملف نصوص واحد** (`src/i18n/ar.ts`) لا مبعثرة في الكود، ليسهل تعديلها لاحقاً.
أضف في الإعدادات مفتاحاً `studentGender` بقيمة `female` افتراضياً، وولّد الصياغة منه — فإن دُرّست فصول بنين مستقبلاً تتغيّر الصياغة تلقائياً دون إعادة بناء.

---

## 2. ما الذي نبنيه؟

تطبيق ويب محلي (Local-first) يعمل **بدون إنترنت** على جهاز المعلّم، يدير:

### لغة المادة — مقرّرة

**العلوم تُدرَّس بالعربية.** كل محتوى المكتبة يُبنى بالعربية: العروض، أوراق العمل، الاختبارات، بطاقات الألعاب، خطط الدروس، تقارير أولياء الأمور.

- المصطلح العلمي يُكتب **بالعربية**، ويجوز وضع المقابل الإنجليزي **بين قوسين وبخط أصغر** عند أول ذكر فقط في العرض التقديمي — تمهيداً للمرحلة الإعدادية، لا أكثر.
- الرموز العلمية العالمية تبقى كما هي (H₂O · °م · كجم · سم³) ولا تُترجم.
- في الاختبارات الرسمية: **بالعربية فقط بلا أي إنجليزية** — التزاماً بشكل الورقة الرسمية.
- خط المحتوى المطبوع: Amiri للاختبارات والشهادات · Tajawal لأوراق العمل والعروض.
- الأرقام: غربية (0123) في الجداول والدرجات · عربية شرقية (٠١٢٣) في الشهادات والمستندات الرسمية.
- عند بناء بنك الأسئلة من كتاب المنهج، استخدم **مصطلحات الكتاب المقرّر حرفياً** — لا مرادفات ولا اجتهاد، حتى لا تتفاجأ الطالبة بمصطلح لم تره.

### هيكل التنقّل الأساسي — إلزامي

التطبيق **مقسّم حسب الفصل**، وهذا هو المسار الرئيسي فيه:

```
الرئيسية  →  الفصول (خامس ١، خامس ٢، خامس ٣ …)  →  طالبات الفصل  →  ملف الطالبة
```

- الفصول **يضيفها المعلّمة بنفسها** بأي اسم تريده (خامس ١ … خامس ٦، أو أسماء أخرى). لا تثبّت عدد الفصول ولا أسماءها في الكود.
- كل فصل يعرض: الاسم، عدد الطالبات، متوسط الفصل.
- **إدخال الأسماء:** ثلاث طرق إلزامية — إضافة طالبة واحدة · **لصق قائمة أسماء (كل اسم في سطر) فتُنشأ ملفاتهنّ دفعة واحدة** · استيراد من ملف Excel.
- ملف الطالبة صفحة واحدة بأربعة تبويبات: **البيانات · الدرجات · الحضور والنقاط · الملاحظات**.
- بيانات الطالبة: الاسم، الرقم في الكشف، الفصل، ولية/ولي الأمر، رقم التواصل، ملاحظات صحية أو خاصة، صورة اختيارية.
- عند نقل طالبة بين فصلين، **تنتقل معها كل بياناتها** (درجات، نقاط، حضور، ملاحظات) ويُسجَّل تاريخ النقل.
- كل شاشة (درجات، حضور، اختبار، تقرير) تبدأ باختيار الفصل، وتتذكّر آخر فصل عملت فيه المعلّمة.

| # | الوحدة | الوصف المختصر |
|---|--------|----------------|
| 1 | الفصول والطالبات | سجل كامل لكل طالبة داخل فصلها |
| 2 | الدرجات | رصد مرن حسب سياسة التقييم القطرية |
| 3 | الحضور | تسجيل يومي بنقرة |
| 4 | النقاط والمكافآت | محرّك تحفيز شهري |
| 5 | الشهادات | توليد جماعي بقوالب |
| 6 | بنك الأسئلة | مصنّف بالوحدة والمستوى المعرفي |
| 7 | **الاختبارات** | كويزات + منتصف + نهاية الفصل بنظام الوزارة |
| 8 | أوراق العمل | من بنك الأسئلة |
| 9 | التحضير وخطط الدروس | نموذج → Word |
| 10 | مكتبة الموارد | فهرسة ملفات المعلّم |
| 11 | التقارير والتصدير | Excel/Word/PDF + تصدير للنظام الرسمي |
| 12 | التحليلات والإنذار المبكر | رسوم + تنبيهات |
| 13 | **قارئ الدرجات بالتصوير** | يحلّ محلّ الإدخال اليدوي بالكامل — §4-ب |
| 14 | **المكتبة الجاهزة** | لكل درس ٧ عناصر مبنية مسبقاً — §4-ج |
| 15 | **وضع الابن** | لوحة تحكّم لابنها يجهّز لها عن بُعد |

---

## 2-ب. قارئ الدرجات بالتصوير — بديل الإدخال اليدوي

### ورقة الرصد الذكية (المسار الموصى به)

1. التطبيق يطبع ورقة فيها **أسماء طالبات الفصل بالترتيب**، أمام كل اسم **مربّع كبير** (لا يقل عن 18×14مم)، وفي أركان الورقة **٤ علامات محاذاة سوداء** + رمز صغير يحمل رقم الفصل ونوع التقييم.
2. تكتب أبلة عفاف الدرجات بالقلم كعادتها.
3. تصوّرها بالموبايل. التطبيق يصحّح الميلان والمنظور من علامات الأركان، ثم **يقتطع كل مربّع على حدة** ويقرأ الرقم داخله.
4. **لا يقرأ الأسماء إطلاقاً** — المطابقة بترتيب الصفوف، والترتيب معروف من الرمز.

### القواعد التقنية

- **شاشة المراجعة إلزامية ولا يجوز تخطّيها.** تعرض: اسم الطالبة (من قاعدة البيانات) + الرقم المقروء في حقل قابل للتعديل + درجة الثقة. لوّن بالذهبي كل رقم ثقته أقل من 85% واكتب بجانبه «تأكّدي». لا تُحفظ درجة واحدة قبل ضغط «اعتمدي».
- **رفض ما يخرج عن الحد:** رقم أكبر من الدرجة العظمى للمكوّن يُعرض فارغاً بتنبيه، لا يُحفظ.
- **الخصوصية:** إن استُخدمت خدمة رؤية خارجية، **اقتطع عمود الأسماء من الصورة قبل الإرسال** وأرسل مربّعات الأرقام فقط. أسماء الطالبات لا تغادر الجهاز أبداً. اذكر ذلك صراحة في شاشة الإعدادات.
- **الصورة الأصلية تُحفظ** مرتبطة بعملية الرصد، ليمكن الرجوع إليها عند أي خلاف.
- **مصادر أخرى مدعومة:** كشف يدوي قديم · PDF من المدرسة · ورقة الاختبار نفسها (يُقرأ حقل «المجموع» من جدول الرصد). في كل الحالات: نفس شاشة المراجعة.
- **التراجع:** كل عملية رصد قابلة للتراجع كوحدة واحدة خلال ٣٠ يوماً.

## 2-ج. المكتبة الجاهزة — تُبنى قبل الاستخدام

لكل درس في المنهج، ابنِ مسبقاً **سبعة عناصر**: عرض PowerPoint · ورقة عمل (+ نسخة إجابات) · لعبة الحصة مع بطاقاتها · تجربة عملية بأدواتها وإرشادات سلامتها · كرت خروج · خطة درس Word بنموذج المدرسة · ورقة رصد مشاركة.

- تُولَّد من كتاب المنهج ووثيقة المعايير في مرحلة البناء، لا عند الطلب.
- الألعاب من قائمة الاثنتي عشرة لعبة المعتمدة (سباق التصنيف · من أنا؟ · صح وخطأ بالحركة · بينجو المصطلحات · كرت الخروج · المحطات العلمية · مسرح العلوم · الخريطة الذهنية · صندوق الأسئلة · سباق السبورة · لغز اليوم · كنز المفردات) — كلها ≤ ٢٠ دقيقة وتناسب ٢٥ طالبة وسن ١٠ سنوات.
- كل عنصر له زر **«اطبعي»** واحد يعمل فوراً بلا حوارات.
- **حزمة الأسبوع:** زر واحد صباح الأحد يطبع كل مواد الأسبوع دفعة واحدة.

## 2-د. سلوك «سابق بخطوة» — مطلوب في الكود نفسه

- الاختبار يُجهَّز **تلقائياً قبل موعده بأسبوعين** ويظهر تنبيه: «جاهز، راجعيه فقط».
- لوحة الشرف تُجهَّز **كل خميس** بلا طلب.
- تقارير أولياء الأمور تُجهَّز **آخر كل شهر** بلا طلب.
- **لا شاشة فارغة أبداً** — كل شاشة تعرض الإجراء المقترح التالي.
- **زر الطوارئ:** حصة بديلة كاملة صالحة لأي درس، بضغطة واحدة.
- **سلّة استرجاع ٣٠ يوماً** — لا حذف نهائي إطلاقاً.
- **تنبيه أغسطس:** «هل تغيّرت سياسة التقييم؟» مع عرض الأرقام الحالية للتأكيد.
- **أسبوع تدريب مدمج:** خطوة واحدة كل يوم في الأيام السبعة الأولى.
- **إدخال صوتي** للأوامر الشائعة: تسجيل غياب، إعطاء نجوم، فتح درس.
- **وضع الابن:** لوحة منفصلة تعرض ما تحتاجه والدته، وتسمح بتجهيز المواد عن بُعد، وترسل **نسخة احتياطية أسبوعية إلى بريده** تلقائياً.

---

## 2-هـ. حدود الذكاء الاصطناعي — قاعدة أمنية غير قابلة للتفاوض

المنصّة **محلية + متصلة بذكاء اصطناعي**، بهذا الخط الفاصل بالضبط:

| النوع | على الجهاز | يُرسل للذكاء الاصطناعي |
|-------|-----------|------------------------|
| أسماء الطالبات · الدرجات المرتبطة بها · الحضور · الملاحظات السلوكية · أرقام أولياء الأمور · صور الطالبات | ✔ دائماً | ✘ **أبداً** |
| كتاب المنهج · الملازم · العروض · أوراق العمل · نصوص الأسئلة | — | ✔ نعم |
| صورة خانات الدرجات | — | ✔ **بعد قصّ عمود الأسماء** |
| أرقام مجرّدة بلا أسماء (للتحليل) | — | ✔ نعم |

> **القاعدة الواحدة: أي شيء فيه اسم بنت — لا يخرج من الجهاز.**

ثلاثة ضمانات إلزامية في الكود:
1. شاشة **«ما سيُرسل»** تعرض المحتوى الفعلي قبل كل إرسال، مع زر إلغاء.
2. **سجل إرسال كامل** (التاريخ، النوع، الحجم) قابل للمراجعة والتصدير.
3. زر **«اقطعي الاتصال»** يوقف كل إرسال خارجي — وتبقى المنصّة تعمل بكل وظائفها الأساسية بلا إنترنت (الدرجات، النقاط، الطباعة، التقارير).

## 2-و. مركز المصادر واستوديو العرض

**مركز المصادر** — تُرفع مرّة واحدة أول العام وتصير مرجع الذكاء الاصطناعي:
- كتاب العلوم للمستوى الخامس · ملازم الأسئلة · الخطط الفصلية · عروضها الحالية · أوراق عملها · قوائم الطالبات · قالب كشف الدرجات المعتمد في المدرسة · الجدول الدراسي.
- الترتيب: **الصف ← الفصل ← الوحدة ← الدرس**.
- **قاعدة إلزامية:** عند توليد أي سؤال أو نشاط أو شرح، الزم النموذج بـ«استخدم الكتاب والملازم المرفوعة فقط» ولا تسمح بمعرفة عامة خارجها. المصطلحات من الكتاب حرفياً.

**استوديو العرض التقديمي** — نبني على عروضها لا بدلاً منها:
- تكتب بالعامية: «أضيفي بعد الشريحة ٦ لعبة جماعية ٧ دقائق عن دورة الماء» → تُعرض الشرائح المقترحة → تراجع → تحفظ.
- **الملف الأصلي لا يُمسّ إطلاقاً.** تُحفظ نسخة جديدة مع سجل تعديلات ورقم إصدار، ويمكن العودة لأي نسخة سابقة.
- نفس المبدأ لأوراق العمل والخطط: تعديل بالكلام + حفظ نسخة + سجل.

## 2-ز. ميزات إضافية معتمدة

- **نواتج التعلّم:** كل سؤال في البنك مربوط بناتج تعلّم. تُشتقّ منه تلقائياً: «أضعف ناتج تعلّم» · «أصعب سؤال» · «عدد الطالبات المحتاجات دعماً».
- **المجموعة العلاجية لا الخطة الفردية:** جمّع الطالبات ذوات **نفس نقطة الضعف** في مجموعة واحدة بخطة واحدة ونشاط متابعة واحد، ثم قِس التحسّن. لا تولّد خطة منفصلة لكل طالبة.
- **قائمة التجهيز الأسبوعي:** عدد النسخ المطلوب طباعتها لكل فصل + أدوات المختبر + **تنبيه بالنواقص قبل الحصة بيومين**.
- **الملاحظات الصوتية بعد الحصة:** تسجيل حرّ بالعامية → استخراج تلقائي لـ (نقاط لطالبات · طالبات يحتجن متابعة · مهام) → عرضها للاعتماد. هذا هو المسار الصوتي الأساسي، لا الأوامر الصوتية.
- **ملف الطالبة الغائبة:** شرح مختصر + ورقة عمل + واجب، في حزمة واحدة بضغطة.
- **عجلة الاختيار العادلة:** استبعاد الغائبات تلقائياً · منع تكرار الاسم حتى تُستوفى القائمة · سجل اختيارات مرئي.
- **سجل نسخ لكل ملف:** لا استبدال لأي أصل، أبداً.
- **مؤجَّل للمرحلة الأخيرة:** الألعاب برابط/QR من أجهزة الطالبات — لا يُعتمد عليها في الحصة (بنات في العاشرة غالباً بلا أجهزة).

## 3. التقنيات المعتمدة — لا تغيّرها

```
الواجهة:        React 18 + TypeScript + Vite
التنسيق:        Tailwind CSS  (dir="rtl" على مستوى <html>)
قاعدة البيانات: Dexie.js فوق IndexedDB   (محلية بالكامل)
الحالة:         Zustand
الرسوم:         Recharts
التوجيه:        React Router
العمل بلا نت:   PWA (vite-plugin-pwa) + service worker
التغليف لاحقاً: Tauri  (المرحلة الأخيرة فقط، لا تبدأ بها)
```

**مكتبات توليد الملفات:**

```
Word   (.docx)  →  docx           (npm: docx)
Excel  (.xlsx)  →  exceljs
PPT    (.pptx)  →  pptxgenjs
PDF             →  طباعة المتصفح  (اقرأ §5 — قاعدة إلزامية)
صور PNG         →  html-to-image
QR              →  qrcode
الكاميرا        →  getUserMedia + <input type="file" capture>
معالجة الصورة   →  OpenCV.js (تصحيح المنظور من علامات الأركان) — محلي بالكامل
قراءة الأرقام   →  Tesseract.js محلياً أولاً؛ وإن ضعفت الدقة، خدمة رؤية
                   مع اقتطاع عمود الأسماء قبل الإرسال (§2-ب)
الصوت           →  Web Speech API (ar-QA) للأوامر الشائعة
```

**ممنوع:** أي خدمة سحابية، أي API خارجي، أي حساب أو تسجيل دخول عن بُعد، أي إرسال لبيانات الطلاب خارج الجهاز. الخطوط والمكتبات كلها محلية (`node_modules` + `public/fonts`) — لا روابط CDN.

---

## 4. سياسة التقييم القطرية — النموذج الافتراضي

الشائع للصفوف ١–٩، **لكل فصل دراسي من ١٠٠ درجة**:

| المكوّن | الدرجة |
|---------|--------|
| اختبار منتصف الفصل | 25 |
| **أعمال الفصل** | **40** |
| ← التقييمات القصيرة | 15 |
| ← المشاركة الصفية | 15 |
| ← الواجبات | 10 |
| اختبار نهاية الفصل | 35 |

- الفصل الأول = **40%** من الدرجة النهائية · الفصل الثاني = **60%**
- النهاية العظمى 100 · النهاية الصغرى (النجاح) 50

### ⚠️ قاعدة حرجة

**هذه الأرقام تتغيّر كل عام.** الوزارة قد تلغي اختبار منتصف الفصل وتستبدله بتقييم إلكتروني بدرجة مختلفة، أو تعدّل الأوزان.

لذلك: **يُمنع منعاً باتاً كتابة أي من هذه الأرقام داخل الكود.** كلها تُقرأ من جدول `assessmentPolicy` في قاعدة البيانات، ويعدّلها المعلّم من شاشة «الإعدادات ← سياسة التقييم» التي يجب أن تسمح بـ:

- إضافة/حذف/إعادة تسمية أي مكوّن تقييم
- تغيير درجة أي مكوّن
- تغيير وزن الفصل الأول والثاني
- حفظ نسخة السياسة لكل عام أكاديمي (فلا تفسد بيانات الأعوام السابقة عند التعديل)

عند فتح الشاشة، تحقّق أن مجموع المكوّنات = 100 وأظهر تحذيراً أحمر إن لم يكن كذلك.

---

## 5. العربية — القواعد التي تمنع 90% من الأخطاء

هذه أهم فقرة تقنية في الملف. اقرأها قبل كل مهمة توليد ملفات.

### PDF — القاعدة الذهبية

**لا تستخدم jsPDF ولا pdfmake ولا pdf-lib لتوليد نصوص عربية.** ستنتج حروفاً مقطّعة ومعكوسة لأنها لا تدعم تشكيل الحروف العربية (Arabic shaping) بشكل موثوق.

**الطريقة الصحيحة الوحيدة:**
1. ابنِ المخرج كصفحة HTML بتنسيق `@media print` و `@page { size: A4; margin: … }`
2. افتحها في نافذة/إطار مخفي واستدعِ `window.print()`
3. المتصفح يشكّل العربية بشكل مثالي، والمستخدم يختار "حفظ كـ PDF"

لأي تصدير مجمّع (٣٠ شهادة مثلاً) اجعلها ٣٠ صفحة في مستند واحد مع `page-break-after: always`.

### Word (docx)

```ts
new Document({
  styles: { default: { document: { run: { font: "Arial", rightToLeft: true } } } },
  sections: [{
    properties: { page: { size: { orientation: "portrait" } } },
    children: [ new Paragraph({ bidirectional: true, alignment: AlignmentType.RIGHT, /* … */ }) ]
  }]
})
```
- `bidirectional: true` على **كل** فقرة، و`rightToLeft: true` على **كل** run.
- الجداول: اعكس ترتيب الأعمدة يدوياً + `visuallyRightToLeft: true` على الجدول.

### Excel (exceljs)

```ts
sheet.views = [{ rightToLeft: true }];
cell.alignment = { horizontal: "right", readingOrder: "rtl", vertical: "middle" };
```
- الأرقام تبقى `horizontal: "center"`.
- عند تصدير كشف للنظام الرسمي: **رتّب الأعمدة تماماً كما في نموذج المدرسة** ولا تضف أعمدة زائدة.

### PowerPoint (pptxgenjs)

```ts
slide.addText(txt, { rtlMode: true, align: "right", fontFace: "Arial" });
```

### الخطوط

نزّل محلياً في `public/fonts/`: **Tajawal** (الواجهة)، **Cairo** (العناوين)، **Amiri** (الشهادات والاختبارات الرسمية).
لا تستخدم روابط Google Fonts — التطبيق يجب أن يعمل بلا إنترنت.

### الأرقام

اجعلها إعداداً: أرقام عربية شرقية (٠١٢٣) أم غربية (0123). الافتراضي: **غربية في الجداول والدرجات** (أوضح للقراءة السريعة)، **عربية شرقية في الشهادات** (أجمل).

### التواريخ

ميلادي بصيغة `يوم/شهر/سنة` + إمكانية إظهار الهجري بجانبه في المستندات الرسمية.

---

## 6. معايير الواجهة — مستخدم عمره ٦٠ سنة

هذه ليست تفضيلات، بل **متطلبات إلزامية**. راجعها قبل تسليم أي شاشة.

| البند | الحد الأدنى |
|-------|-------------|
| حجم الخط الأساسي | 18px (وزر في الإعدادات يرفعه إلى 20/22/24) |
| ارتفاع أي زر أو حقل | 48px |
| المسافة بين الأزرار | 12px على الأقل |
| تباين النص مع الخلفية | 4.5:1 (WCAG AA) — تحقّق فعلياً، لا تخمّن |
| عمق التنقّل | أي شاشة على بُعد **٣ نقرات كحد أقصى** من الرئيسية |
| أيقونة بلا نص | ممنوعة — كل أيقونة معها كلمة عربية |

قواعد إضافية:

- **لا تعتمد على اللون وحده** لنقل معلومة (ناجح/راسب): أضف رمزاً ونصاً.
- **زر تراجع** بعد كل عملية حفظ مهمة (Toast فيه "تراجع" لمدة 10 ثوانٍ).
- **تأكيد قبل الحذف** دائماً، مع ذكر اسم المحذوف صراحة: «هل تريد حذف الطالب أحمد علي؟».
- **حفظ تلقائي** أثناء الكتابة + مؤشّر «تم الحفظ ✓». لا تجعل المعلّم يخاف من فقدان عمله.
- **رسائل الخطأ بالعربية وبلغة بشرية**: «لم أستطع حفظ الدرجة — تأكد أن الرقم بين 0 و 25» — لا "Error 500".
- **شاشات فارغة مفيدة**: بدل صفحة بيضاء، اكتب «لا يوجد طلاب بعد» + زر كبير «استيراد قائمة من Excel».
- **الكيبورد أولاً في الرصد**: Enter ينتقل للطالب التالي، Tab للمكوّن التالي، Esc يلغي. الفصل كله في شاشة واحدة بلا تمرير أفقي.

---

## 7. الأمان والبيانات

- كل البيانات في IndexedDB على جهاز المعلّم. **لا شيء يخرج من الجهاز.**
- زر **«نسخة احتياطية كاملة»** يصدّر ملف `backup-YYYY-MM-DD.json` واحد يحوي كل شيء.
- زر **«استعادة نسخة»** يستورده ويستبدل البيانات (مع تأكيد مزدوج).
- تذكير تلقائي كل ٧ أيام: «آخر نسخة احتياطية كانت قبل ٧ أيام — احفظ نسخة الآن».
- قفل اختياري برقم سرّي عند فتح التطبيق.
- **قبل أي عملية استيراد أو حذف جماعي، خذ نسخة احتياطية تلقائية صامتة.**
- الملفات الكبيرة (عروض، فيديو) تبقى في مجلد على الجهاز؛ نخزّن **المسار فقط** في قاعدة البيانات لا الملف.

---

## 8. أسلوب العمل معك أنت (Claude Code)

1. **مرحلة واحدة في كل مرة.** لا تبدأ المرحلة التالية قبل أن يقول المستخدم «تمام».
2. **بعد كل مرحلة:** شغّل `npm run build` وتأكد أنه ينجح، ثم اعمل commit برسالة عربية واضحة.
3. **اعمل commit تلقائياً** بعد كل خطوة ناجحة. المستخدم لا يعرف git — أنت مسؤول عنه بالكامل. إن قال «ارجع للخلف» فنفّذ `git revert` أو `git checkout` بنفسك.
4. **بيانات تجريبية:** في كل مرحلة أنشئ بيانات وهمية (٣ فصول × ٢٥ طالباً بأسماء عربية واقعية) ليجرّب المستخدم فوراً. اجعل زر «مسح البيانات التجريبية» ظاهراً في الإعدادات.
5. **دليل الاستخدام:** بعد كل مرحلة، حدّث `دليل-الاستخدام.md` بالعربية، بلغة بسيطة وبخطوات مرقّمة وبدون مصطلحات تقنية.
6. **الاختبارات:** اكتب اختبارات (Vitest) لكل ما فيه حساب: معادلة الدرجة النهائية، محرّك النقاط، توزيع أسئلة الاختبار. الأخطاء الحسابية هنا تصل إلى شهادة طالب — لا تتساهل.
7. **لا تعقّد.** لو كان هناك حلّان، اختر الأبسط الذي يفهمه أي مبرمج بعدك. هذا المشروع يجب أن يبقى صالحاً للتعديل سنوات.
8. **لا تحذف بيانات المستخدم أبداً** أثناء أي تعديل أو ترقية. اكتب migrations لتغييرات قاعدة البيانات.

---

## 9. المهارات (Skills) — استخدمها ولا تتجاهلها

| المهارة | متى |
|---------|-----|
| `ui-ux-design-system` | قبل تصميم أي شاشة جديدة |
| `frontend-design` | عند بناء الواجهات |
| `design:accessibility-review` | بعد كل مرحلة واجهة — إلزامي (المستخدم ٦٠ سنة) |
| `design:ux-copy` | لصياغة نصوص الأزرار ورسائل الخطأ بالعربية |
| `docx` | قبل كتابة أي كود يولّد Word |
| `xlsx` | قبل أي كود يولّد Excel |
| `pptx` | قبل أي كود يولّد PowerPoint |
| `pdf` | لفهم آليات PDF (مع الالتزام بقاعدة §5) |
| `dataviz` | قبل بناء أي رسم بياني أو لوحة تحليلات |
| `canvas-design` | لتصميم قوالب الشهادات |
| `engineering:system-design` | المرحلة صفر — هيكل البيانات |
| `engineering:testing-strategy` | لخطة اختبار المعادلات الحسابية |
| `engineering:code-review` | قبل نهاية كل مرحلة |
| `engineering:debug` | عند أي خطأ متكرر |
| `engineering:documentation` | لكتابة دليل الاستخدام العربي |
| `skill-creator` + `agent-skills-guide` | لبناء مهارات المشروع الخاصة (§10) |

---

## 10. مهارات المشروع الخاصة

في المرحلة السابعة، أنشئ هذه المهارات داخل `.claude/skills/` حتى يستطيع المعلّم لاحقاً أن يكتب جملة عربية واحدة فيحصل على المخرج كاملاً:

| المجلد | ما تفعله | يقولها المعلّم هكذا |
|--------|----------|---------------------|
| `create-exam/` | تبني اختباراً كاملاً بنظام الوزارة + نموذج إجابة + جدول مواصفات | «اعمل اختبار نهاية الفصل للوحدة ٢ و ٣» |
| `create-quiz/` | تقييم صفي قصير من ٥–١٠ أسئلة | «اعمل كويز ٥ دقائق على درس دورة الماء» |
| `create-worksheet/` | ورقة عمل منسّقة بالترويسة | «اعمل ورقة عمل على الجهاز الهضمي» |
| `create-certificate/` | شهادة تقدير لطالب أو لفصل | «اطبع شهادات لأوائل الشهر» |
| `lesson-plan/` | خطة درس بنموذج المدرسة → Word | «حضّرلي درس الغد عن المغناطيس» |
| `parent-report/` | بطاقة متابعة PDF لولي الأمر | «اعمل تقرير لولي أمر سارة» |
| `official-export/` | كشف Excel بترتيب أعمدة النظام الرسمي | «جهّز كشف الدرجات للنظام» |

كل مهارة يجب أن:
- تقرأ البيانات الحقيقية من قاعدة بيانات التطبيق (عبر سكربت تصدير JSON)
- تلتزم بقواعد العربية في §5
- تُخرج الملف في مجلد `المخرجات/` مع اسم واضح بالعربية والتاريخ
- تنتهي برسالة عربية من ٣ أسطر للمعلّم

---

## 11. أخطاء شائعة — تجنّبها مسبقاً

- ❌ كتابة درجات سياسة التقييم داخل الكود → ✅ من قاعدة البيانات
- ❌ توليد PDF عربي بمكتبة JS → ✅ طباعة المتصفح
- ❌ خط 14px وأزرار صغيرة → ✅ 18px و 48px
- ❌ سؤال المستخدم سؤالاً تقنياً → ✅ قرّر ونفّذ
- ❌ حذف بيانات عند الترقية → ✅ migration
- ❌ ربط بخدمة سحابية "لتسهيل المزامنة" → ✅ ممنوع تماماً
- ❌ الاعتماد على الإنترنت لخط أو أيقونة → ✅ كل شيء محلي
- ❌ إظهار رسالة خطأ إنجليزية → ✅ عربية وبشرية
